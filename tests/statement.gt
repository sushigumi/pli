# if else statement
proc main()
    int a;
    int b;
begin
    a := 2;
    b := 4;
    if a > b then
        write "a is greater than b\n";
    fi
    if a >= b then
        write "a is greater than or equal to b\n";
    else
        write "a is less than b\n";
    fi
end
