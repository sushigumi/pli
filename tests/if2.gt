# Compute the mean and variance of a list of numbers

proc main ()
    int n;
    bool done;
begin
    n := 3;
    done := n <= 0;
    if done then
        n := n - 1;
    fi
    write n;
    write "\n";
end

